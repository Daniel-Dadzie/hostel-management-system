-- Create the hostel_db database if it doesn't exist
CREATE DATABASE IF NOT EXISTS hostel_db;

-- Use the database
USE hostel_db;

-- Show that we're connected
SELECT 'Database hostel_db is ready!' AS status;
